fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
python-multipart==0.0.9
httpx>=0.28.1,<1.0.0
google-genai>=2.0.0,<3.0.0
python-dotenv>=1.0.1,<2.0.0

# Add these optional heavy dependencies only when needed. They are omitted by
# default to keep the base image and build time small:
# faster-whisper
# piper-tts
